# Release Notes

## 2026-06-09

- Windows release packaging now publishes a ready-to-run installer `.exe` for both nightly and tagged releases.
- The installer is uploaded alongside the existing Windows archive and checksum assets.
- Tagged releases continue to publish the versioned installer name, while nightly keeps the rolling `windows-installer.exe` asset.
